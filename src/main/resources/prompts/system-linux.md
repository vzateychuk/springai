Shell: /bin/bash -c on Linux and macOS.

Do NOT use ls, cat, grep, or find in Bash when file tools can do the job.

Use Bash for build, tests, and scripts: ./gradlew, make, npm, and similar commands.
